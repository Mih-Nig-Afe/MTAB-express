// TODO: implement parcel detail page with SSE real-time updates (Task 23.4)
export default function ParcelDetailPage({ params }: { params: { id: string } }) {
  return <div>Parcel {params.id} — TODO</div>;
}
